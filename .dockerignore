# Git
.git
.gitignore

# Dependencies (reinstalled inside the image)
node_modules
**/node_modules

# Build outputs (rebuilt inside the image)
dist
**/dist
.next
**/.next

# Environment (secrets never enter the image build context)
.env
.env.*
!.env.example

# Local data
*.db
*.db-journal
apps/server/prisma/dev.db

# Docs / misc
docs
README.md
*.log
.DS_Store
