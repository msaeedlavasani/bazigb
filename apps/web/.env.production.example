# BaziGB Web environment (production)
# These are inlined at BUILD time by Next.js — set them as Docker build args.

# Same-origin routing: keep empty. Caddy proxies /api/* and /socket.io/* to the server.
NEXT_PUBLIC_API_URL=""
NEXT_PUBLIC_SOCKET_URL=""
