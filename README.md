# BaziGB - پلتفرم بازی‌های رومیزی آنلاین

**BaziGB** یک پلتفرم مدرن، ماژولار و دیتابیس‌محور برای بازی‌های رومیزی آنلاین است که با تمرکز بر توسعه‌پذیری و پرفورمنس طراحی شده است.

## وضعیت پروژه: بچ ۸ تکمیل شد ✅
پروژه اکنون دارای زیرساخت کامل دیتابیس (Prisma)، سیستم احراز هویت (JWT)، سیستم مسابقات حذفی و لیدربورد، و همچنین زیرساخت مدیریت دارایی‌های صوتی (Sound SFX) است. سه بازی دوز، شطرنج و تخته‌نرد به صورت کامل قابل بازی هستند.

## قابلیت‌های کلیدی
- **انجین بازی اختصاصی:** مدیریت نوبت‌ها و وضعیت بازی به صورت متمرکز.
- **معماری پلاگین‌محور:** اضافه کردن بازی‌های جدید (مثل وگاس) بدون دست زدن به کدهای هسته.
- **ارتباط زنده (Real-time):** استفاده از Socket.io برای همگام‌سازی لحظه‌ای بازیکنان.
- **پایداری (Persistence):** ذخیره دائم اطلاعات کاربران، اتاق‌ها و تاریخچه مسابقات در دیتابیس.
- **احراز هویت امن:** استفاده از JWT و هش کردن پسوردها با Bcrypt.
- **سیستم رنکینگ و مسابقات:** محاسبه امتیاز ELO و برگزاری مسابقات حذفی خودکار.
- **زیرساخت صوتی:** پخش هوشمند افکت‌های صوتی بازی برای تجربه کاربری بهتر.

## ساختار پروژه
این پروژه به صورت یک **Monorepo** مدیریت می‌شود:
- **`apps/web`**: رابط کاربری (Next.js + TailwindCSS).
- **`apps/server`**: سرویس‌های بک‌اِند (NestJS + Prisma).
- **`packages/engine`**: هسته منطق بازی‌ها.
- **`packages/games`**: پکیج‌های اختصاصی بازی‌ها (دوز، و در آینده شطرنج و غیره).

## مستندات
- [نقشه راه پروژه (Roadmap)](docs/ROADMAP.md) - مسیر توسعه تا فاز ۵.
- [لیست ایشوها و باگ‌ها (Issues)](docs/ISSUES.md) - ردیابی بدهی‌های فنی.
- [معماری فنی (Architecture)](docs/ARCHITECTURE.md) - جزئیات مدل Engine/Plugin.
- [مستند هندآف (Hand-off)](docs/HANDOFF.md) - وضعیت دقیق فعلی برای توسعه‌دهندگان.
- [راهنمای توسعه بازی (SDK)](docs/GAME_SDK.md) - چطور یک بازی جدید اضافه کنیم؟

## نحوه اجرا و توسعه
۱. نصب تمام وابستگی‌ها:
```bash
npm install
```

۲. تنظیم دیتابیس (Prisma):
در پوشه `apps/server` فایل `.env` را چک کرده و سپس:
```bash
npx prisma generate
```

۳. اجرای پروژه‌ها:
```bash
# فرانت‌اِند
npm run dev:web

# سرور
npm run dev:server
```

## هدف نهایی
ایجاد اکوسیستمی که هر توسعه‌دهنده‌ای بتواند بازی رومیزی خود را به راحتی به پلتفرم BaziGB اضافه کرده و از قابلیت‌های چندنفره، رنکینگ و چت بهره‌مند شود.
