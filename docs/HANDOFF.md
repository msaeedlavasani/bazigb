# مستند هندآف (Hand-off) پروژه BaziGB

تاریخ: ۱۶ مرداد ۱۴۰۵
وضعیت پروژه: بچ ۸ تکمیل شد - زیرساخت صوتی و تخته‌نرد عملیاتی هستند

## وضعیت فعلی
پلتفرم BaziGB اکنون دارای سیستم مدیریت دارایی‌های صوتی (Asset Pipeline) و سه بازی فعال است. زیرساخت برای بازی‌های پیچیده‌تر و مبتنی بر شانس (تاس) کاملاً آماده است.
- **Engine Core:** پشتیبانی از سیستم RNG امن (تاس) و فازهای مختلف بازی در یک نوبت.
- **Sound Service:** مدیریت متمرکز افکت‌های صوتی با قابلیت Preload و تنظیمات Mute جهانی.
- **Backgammon Plugin:** پیاده‌سازی منطق پایه و چیدمان مهره‌های تخته‌نرد.
- **Chess & Tic-Tac-Toe:** ارتقا یافته با افکت‌های صوتی حرکت و زدن مهره.
- **Social & Competitive:** رنکینگ ELO، چت زنده و Spectator Mode فعال هستند.

## کارهای انجام شده
- [x] پیاده‌سازی سرویس صوتی و دکمه کنترل صدا در نوار ناوبری.
- [x] اضافه کردن افکت‌های صوتی تاس، حرکت و کپچر.
- [x] پیاده‌سازی کامل منطق و رابط کاربری تخته‌نرد (بنیان‌گذاری بازی‌های تاس‌محور).
- [x] راه‌اندازی سیستم مسابقات حذفی و لیدربورد جهانی.

## گام‌های بعدی پیشنهادی
۱. **بازی وگاس (Vegas):** پیاده‌سازی این بازی استراتژیک تاس‌محور (اولویت اول فاز ۳).
۲. **تکمیل تعاملات تخته‌نرد:** نهایی کردن سیستم Drag & Drop و قوانین پیشرفته حرکت.
۳. **نوتفیکیشن‌های پوش:** اطلاع‌رسانی نوبت بازی به کاربران.
۴. **بهبود انیمیشن‌ها:** استفاده از فیزیک برای حرکت طبیعی‌تر تاس‌ها.

## پروتکل عملیاتی BaziGB (مهم برای هوش مصنوعی)

برای حفظ نظم و جلوگیری از شلوغی Context، تمام تسک‌ها باید طبق این پروتکل انجام شوند:

۱. **دسته بندی (Batching):** تسک‌ها بر اساس نقشه راه (Roadmap) به گروه‌های حداکثر ۵تایی تقسیم می‌شوند.
۲. **برون‌سپاری (Delegation):** انجام تسک‌ها به ساب‌ایجنت‌ها (`sessions_spawn`) سپرده می‌شود. هر ساب‌ایجنت مسئول یک خروجی مشخص و مستقل است.
۳. **تأیید و ادغام (Verify & Merge):** پس از اتمام کار ساب‌ایجنت‌ها، ایجنت اصلی نتایج را وریفای کرده و با کد اصلی مرج می‌کند.
۴. **بروزرسانی مستندات:** پس از هر بچ، تمام مستندات فنی و فایل `HANDOFF.md` بر اساس تغییرات جدید آپدیت می‌شوند.
۵. **ثبت نهایی (Push):** پس از تأیید نهایی هر بچ ۵تایی، کدها به مخزن GitHub پوش می‌شوند.

---

## نکات فنی برای توسعه‌دهنده بعدی
- برای اجرای کل پروژه، در ریشه دستور `npm install` را بزنید.
- منطق بازی‌ها را فقط در `packages/games` تغییر دهید.
- انجین در حال حاضر بسیار ساده است و نیاز به سیستم `Undo` و `Redo` دارد.
