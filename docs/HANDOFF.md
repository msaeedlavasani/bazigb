# مستند هندآف (Hand-off) پروژه BaziGB

تاریخ: ۱۶ مرداد ۱۴۰۵
وضعیت پروژه: بچ ۱ تکمیل شد - بازی دوز آنلاین عملیاتی است

## وضعیت فعلی
پلتفرم BaziGB اکنون دارای یک انجین بازی نوبتی فعال است و اولین بازی (دوز) به صورت کامل و چندنفره قابل بازی است.
- **Engine Core:** دارای متدهای `createInitialState` و `processMove` برای مدیریت منطق بازی و نوبت‌ها.
- **Tic-Tac-Toe Plugin:** پیاده‌سازی کامل منطق بازی و تشخیص برنده/مساوی.
- **Server Socket:** مدیریت اتاق‌ها در حافظه، همگام‌سازی وضعیت (State Sync) و Broadcast حرکت‌ها.
- **Web UI:** رابط کاربری مدرن با استفاده از TailwindCSS و اتصال زنده به سرور.

## کارهای انجام شده
- [x] تنظیم ساختار Monorepo و CI (Node 24).
- [x] پیاده‌سازی هسته انجین بازی‌های نوبتی.
- [x] پیاده‌سازی کامل فرانت‌اِند و بک‌اِند بازی دوز.
- [x] تنظیم پروتکل عملیاتی دلیگیت و بچینگ.

## گام‌های بعدی پیشنهادی
۱. **سیستم اتاق‌های پیشرفته:** امکان ساخت اتاق با کد دعوت و لیست کردن اتاق‌های عمومی.
۲. **شطرنج (Chess):** اضافه کردن بازی شطرنج به عنوان دومین پلاگین.
۳. **دیتابیس:** اضافه کردن PostgreSQL/Prisma برای ذخیره کاربران و تاریخچه مسابقات.
۴. **Spectator Mode:** امکان تماشای بازی‌های در حال جریان توسط کاربران دیگر.
۵. **سیستم رنکینگ (ELO):** محاسبه امتیاز بازیکنان بر اساس نتایج مسابقات.

## پروتکل عملیاتی BaziGB (مهم برای هوش مصنوعی)

برای حفظ نظم و جلوگیری از شلوغی Context، تمام تسک‌ها باید طبق این پروتکل انجام شوند:

۱. **دسته بندی (Batching):** تسک‌ها بر اساس نقشه راه (Roadmap) به گروه‌های حداکثر ۵تایی تقسیم می‌شوند.
۲. **برون‌سپاری (Delegation):** انجام تسک‌ها به ساب‌ایجنت‌ها (`sessions_spawn`) سپرده می‌شود. هر ساب‌ایجنت مسئول یک خروجی مشخص و مستقل است.
۳. **تأیید و ادغام (Verify & Merge):** پس از اتمام کار ساب‌ایجنت‌ها، ایجنت اصلی نتایج را وریفای کرده و با کد اصلی مرج می‌کند.
۴. **بروزرسانی مستندات:** پس از هر بچ، تمام مستندات فنی و فایل `HANDOFF.md` بر اساس تغییرات جدید آپدیت می‌شوند.
۵. **ثبت نهایی (Push):** پس از تأیید نهایی هر بچ ۵تایی، کدها به مخزن GitHub پوش می‌شوند.

---

## نکات فنی برای توسعه‌دهنده بعدی
- برای اجرای کل پروژه، در ریشه دستور `npm install` را بزنید.
- منطق بازی‌ها را فقط در `packages/games` تغییر دهید.
- انجین در حال حاضر بسیار ساده است و نیاز به سیستم `Undo` و `Redo` دارد.
