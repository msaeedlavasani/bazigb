# مستند هندآف (Hand-off) پروژه BaziGB

تاریخ: ۱۹ مرداد ۱۴۰۵
وضعیت پروژه: **بچ ۱۴ تکمیل شد** (OTP پیامکی فعال شد — پنل sms.ir وصل شد؛ ورود با شماره موبایل؛ زیرساخت RBAC و پنل مدیریتی اولیه) + بچ ۱۲ و ۱۳ تکمیل شده (MUI Design System، یوزرنیم در بازی‌ها، دکمه Share، ادیت پروفایل، سیستم Undo، سخت‌سازی امنیتی، فیکس CI). **بچ ۱۵ (UI/UX و برند) در برد تسک ثبت شده — آماده شروع.**

## بچ ۱۵ — UI/UX و برند (آماده، شروع نشده — ۲۰ مرداد)

تسک‌های برد (شماره‌های تسک در پنل): **ترتیب پیشنهادی اجرا: ۱۰ → ۱۳، ۱۲ → ۹، سپس ۱۴ و ۱۱ (موازی).**

- [ ] **(تسک ۱۰) اتو-لیوت مشترک (هدر/محتوا/فوتر):** ساختار AppShell با flex column و `minHeight: 100vh` تا فوتر همیشه پایین بچسبد؛ هدر/فوتر یک‌بار در لیوت ریشه رندر شوند نه در هر صفحه. **پیش‌نیاز تسک‌های ۹، ۱۲، ۱۳.**
- [ ] **(تسک ۱۳) اصلاح هدر (Nav) برای اتو-لیوت:** sticky در بالا، هم‌تراز با کانتینر محتوا و فوتر، حذف استایل‌های متناقض بین صفحات. فایل: `apps/web/src/app/components/Nav.tsx`.
- [ ] **(تسک ۱۲) افزودن فوتر + ویرایش از پنل ادمین:** مدل Prisma جدید (مثلاً `SiteSettings` با `footerText`/`footerLinks` JSON) + مایگریشن؛ `GET /site-settings` (عمومی) و `PATCH /admin/site-settings` (فقط ADMIN)؛ کامپوننت `Footer.tsx`؛ بخش ویرایش در `/admin`. محتوا از DB خوانده شود تا بدون دیپلوی تغییر کند.
- [ ] **(تسک ۹) بررسی و اصلاح ریسپانسیو:** همه صفحات در 360/768/1440 — بدون overflow افقی، بریک‌پوینت‌های درست (بعد از اتو-لیوت انجام شود).
- [ ] **(تسک ۱۴) پالت رنگی برند:** پنج رنگ رسمی — **Autumn Ember `#B25D16`**، **Dark Teal `#114B5E`**، **Orange `#F5A306`**، **Ash Grey `#BEBBAC`**، **Ink Black `#030A15`**. تم MUI (`ThemeRegistry.tsx`) با این‌ها به‌روز شود (پیشنهاد: primary=B25D16، secondary=114B5E، background=030A15، Ash Grey برای متن/مرز خاکستری، Orange برای accent). رنگ‌های هاردکد بیربط (مثل `#6366f1`, `#0ea5e9`, `#0f172a`, `#1e293b`) جارو و با توکن‌ها جایگزین شوند؛ گرادیان‌های indigo→sky با گرادیان پالت برند عوض شوند. **قانون: برای error/warning/info/success فقط از دیفالت MUI استفاده شود.**
- [ ] **(تسک ۱۱) یکدست‌سازی نام برند BaziG3 → BaziGB:** ۵ مورد — `Nav.tsx:84,104`، `layout.tsx:7`، `page.tsx:71,89`. grep باید صفر شود.
- [ ] **(تسک ۱۵) لوگوی رسمی ترنسپرنت:** کاربر لوگوی رسمی (جیب جین + BaziG3) را داد — ذخیره‌شده در `apps/web/public/brand/logo-transparent.webp`. باید در هدر، hero صفحه اصلی، صفحات auth و فوتر استفاده شود (+ favicon). ⚠️ دو نکته: (۱) خروجی CDN شفافیت ندارد (`hasAlpha: no`) — یا فایل اصلی PNG را بگیریم یا پس‌زمینه را حذف کنیم؛ (۲) داخل لوگو «BaziG3» نوشته شده — با تسک ۱۱ تناقض دارد، تصمیم با کاربر.

نکته: تسک‌های ۱۴ و ۱۱ مستقل‌اند و می‌توانند هم‌زمان با بقیه پیش بروند.

## بچ ۱۴ — هویت و مدیریت (تکمیل شد — ۱۹ مرداد)

- [x] **فعال‌سازی OTP پیامکی (تسک ۳۰):** پنل sms.ir فعال شد؛ کلید Production و شناسه قالب `997360` در `apps/server/.env` تنظیم شد (`SMSIR_API_KEY`, `SMSIR_TEMPLATE_ID`). نام پارامتر قالب در `sms.service.ts` به `OTP` اصلاح شد (مطابق قالب `#OTP#` — قبلاً `Code` بود). وریفای واقعی: `POST /auth/otp/request` → `{"sent":true}` و لاگ `OTP sent to ... (messageId from sms.ir)` از سرور.
- [x] **ورود با موبایل در وب (تسک ۳۱):** تب «ایمیل / شماره موبایل» در صفحه لاگین (MUI)؛ اعتبارسنجی شماره `09xxxxxxxxx`؛ دریافت کد ۶ رقمی + تایمر ۶۰ ثانیه‌ای ارسال مجدد؛ مدیریت خطای 429؛ جریان کاربر جدید (دریافت یوزرنیم لاتین هنگام `isNewUser`). متد `loginWithOtp` در `AuthContext` — توکن فقط وقتی ذخیره می‌شود که حساب واقعاً وجود داشته باشد (`!isNewUser`).
- [x] **زیرساخت RBAC (تسک ۳۲):** فیلد `role String @default("USER")` به مدل `User` (مقادیر `USER`/`ADMIN` — به‌دلیل SQLite از String به‌جای enum استفاده شد)؛ نقش در JWT (`buildAuthResponse`)؛ دکوراتور `@Roles` + `AdminGuard` در `apps/server/src/common/`؛ ماژول `Admin` با اندپوینت‌های محافظت‌شده.
- [x] **داشبورد ادمین (تسک ۳۳):** صفحه `/admin` (وب) با حفاظت نقش (ردی‌رکت کاربر غیر-ادمین)؛ کارت‌های آمار (کاربران، اتاق‌های فعال، بازی‌های انجام‌شده)؛ جدول کاربران با جستجو، فیلتر نقش و صفحه‌بندی. سمت سرور: `GET /admin/stats` (آمار واقعی با Prisma) و `GET /admin/users` (جستجو روی username/email/phone + فیلتر role + take/skip). **نکته:** `mode: 'insensitive'` در SQLite پشتیبانی نمی‌شود — جستجو با `contains` ساده انجام شد.
- [x] **تست و وریفای (تسک ۳۴):** گارد ادمین تست شد — بدون توکن 401، کاربر عادی 403، ادمین 200 با آمار واقعی. `/admin/users` با فیلتر q/role کار می‌کند. typecheck هر دو پکیج (web و server) پاس است.
- [x] **فیکس جریان ثبت‌نام OTP (مهم):** قبلاً تایید کد برای شماره جدیدِ بدون یوزرنیم **خطای 400** می‌داد و فیلد یوزرنیم در UI هرگز نمایش داده نمی‌شد. حالا سرور `{ isNewUser: true }` برمی‌گرداند تا کلاینت فرم یوزرنیم را نشان دهد. کد OTP هم تا ساخت حساب معتبر می‌ماند (بعد از اولین تایید حذف نمی‌شود — فقط بعد از ساخت حساب/ورود مصرف می‌شود). لاگ `SmsService` حالا messageId واقعی sms.ir را ثبت می‌کند. فلو کامل تست زنده شد (verify بدون یوزرنیم → isNewUser → verify با یوزرنیم → ساخت حساب + JWT).
- [x] **فیکس بیلد وب (MUI):** `@mui/material@9.3.1` به تابع `unstable_createUseMediaQuery` از `@mui/system` نیاز داشت که در هیچ نسخه‌ای منتشر نشده (آخرین system = 9.3.0) → بیلد با خطا می‌شکست. با پین کردن `@mui/material` و `@mui/icons-material` به **9.3.0** (هم‌تراز با system) در `apps/web/package.json` حل شد. `npm run build` ریشه (ترتیبی) حالا سبز است.

### نکات برای ادامه
- برای دسترسی به پنل ادمین، نقش کاربر موردنظر را در DB به `ADMIN` تغییر دهید: `UPDATE users SET role='ADMIN' WHERE username='...'` (سپس دوباره لاگین کنید تا JWT جدید با role بگیرد).
- `.env` سرور حاوی کلید واقعی sms.ir است — در git کامیت نشود.
- **ادمین اصلی:** `boxavier` / `msaeedlavasani@gmail.com` (ایمیل از `saeedlavasani@gmail.com` اصلاح شد) — role=ADMIN. رمز موقت `Boxavier@1405!` (قابل تغییر بعداً؛ سیستم تغییر رمز هنوز ساخته نشده).
- **ری‌استارت سرور ۳۰۰۱:** `bash scripts/restart-server.sh` (اسکریپت کمکی — سرور را می‌کشد و دوباره با کد جدید بالا می‌آورد؛ لاگ در `/tmp/bazigb_server_3001.log`).

## دیپلوی پروداکشن (بچ ۱۴ — انجام شد، ۱۹ مرداد)

- **سرور پروداکشن:** `193.151.153.204` (root) — دامنه `https://bazigb.ir` (Caddy). پروژه در `/opt/bazigb` (clone قدیمی؛ کلید SSH سرور به GitHub وصل نیست — دیپلوی با **rsync مستقیم از ماشین لوکال** انجام می‌شود).
- **دیپلوی‌شده:** مایگریشن `20260810000000_add_user_role` اعمال شد؛ AdminModule فعال (آمار: ۲۹ کاربر، ۸۶ اتاق، ۴۷ بازی)؛ OTP با کلید Production کار می‌کند (`{"sent":true}` از `https://bazigb.ir/api/auth/otp/request`).
- **فیکس مهم Dockerfile.web:** نصب `@next/swc-linux-x64-gnu` با `npm install --no-save` در npm 11 پکیج‌های nested ورک‌اسپیس (مثل `@mui/material` در `apps/web/node_modules`) را prune می‌کرد و بیلد با خطای `unstable_createUseMediaQuery is not a function` می‌شکست. **راه‌حل:** `node scripts/fetch-swc.mjs` (دانلود مستقیم tarball) به‌جای npm install + هم‌ترازی نسخه swc با next (14.2.35). اگر دیپلوی بعدی بیلد وب شکست، اول همین را چک کنید.
- **فیکس OTP در پروداکشن (مهم):** `SMSIR_API_KEY`/`SMSIR_TEMPLATE_ID` باید در بخش `environment` سرویس server در `docker-compose.yml` هم تعریف شوند — فقط در `.env` کافی نیست. بدون آن، کانتینر کلید را نمی‌گیرد و `SmsService` در **dev-mode** می‌افتد (کد فقط در لاگ نوشته می‌شود: `[OTP dev-mode] verification code for ...` — پیامک ارسال نمی‌شود). اگر OTP کار نکرد، اول لاگ سرور را برای این الگو چک کنید.
- **روش دیپلوی فعلی:** (۱) تغییرات را در لوکال کامیت/پوش کنید؛ (۲) rsync به سرور: `rsync -az --delete --exclude={.git,node_modules,.next,dev.db,.env,dist} ./ root@193.151.153.204:/opt/bazigb/`؛ (۳) روی سرور: `docker compose build server web && docker compose up -d --force-recreate server web` (سرور هنگام بالا آمدن مایگریشن‌ها را اعمال می‌کند).

## تکمیل MUI (انجام شد — ۲۰ مرداد)

- [x] **بازبینی کامل کامیت `e052459a` (Batch 14 MUI، ۱۱ فایل):** بیلد web سبز، صفر خطای کنسول در تمام صفحات پروداکشن (تست بصری با مرورگر: home، lobby، leaderboard، tournaments، صفحه خطای اتاق، اتاق بازی، موبایل)، کیفیت کد بالا (بوردها، چت، بازی، لیدربورد همگی MUI).
- [x] **تکمیل تبدیل MUI:** `apps/web/src/app/profile/page.tsx` (قبلاً عمدتاً Tailwind خام بود — حالا کامل MUI: کارت هویت، ۴ کارت آمار، جدول تاریخچه با بج‌های رنگی، اسکلتون، حالت‌های loading/not-signed-in) و `apps/web/src/app/(auth)/layout.tsx` (سرصفحه گرادیانی BaziGB + زیرعنوان، هم‌رنگ با تم). ایمپورت‌های بلااستفاده صفحه اصلی (`Container`, `useTheme`) حذف شد.
- [x] **فیکس بلاکر دیپلوی وب (مهم — بار بعدی حتماً یادتان باشد):** `scripts/fetch-swc.mjs` تاربال `@next/swc-linux-x64-gnu-14.2.35.tgz` را دانلود می‌کرد که **در رجیستری وجود ندارد** (بسته‌های swc پلتفرمی فقط تا **14.2.33** منتشر شده‌اند؛ next@14.2.35 هم خودش به swc@14.2.33 وابسته است). نتیجه: `docker compose build web` با `http 404` شکست می‌خورد. فیکس: URL به `...-14.2.33.tgz` تغییر کرد. **قانون:** نسخه swc در اسکریپت باید با `optionalDependencies` پکیج `next` هم‌تراز باشد، نه با نسخه خود `next`.
- [x] **دیپلوی شد:** rsync + `docker compose build web` + `up -d --force-recreate web`. وریفای زنده: `/profile` (کارت هویت + آمار + تاریخچه)، `/login`، `/register` (لایه auth جدید) — همگی ۰ خطای کنسول. کامیت‌ها: `6f7df83a` (تکمیل MUI)، `541aa226` (فیکس swc).

## بچ ۱۲ — Design System MUI + تجربه کاربری (تکمیل شد — ۱۹ مرداد)

- [x] **MUI Design System (تسک ۲۹):** `@mui/material@9.3.1` + Emotion + icons نصب شد (سازگار با React 18.3 — بدون نیاز به ارتقای React/Next). `ThemeRegistry` (تم تیره، پرایمری ایندیگو `#6366f1`) در layout اضافه شد؛ Nav و فرم‌های ورود/ثبت‌نام به MUI منتقل شدند (همان منطق/هوک‌ها). مهاجرت بقیه صفحه‌ها (لابی/پروفایل/تورنمنت/بردها) تدریجی در بچ‌های بعد.
- [x] **یوزرنیم در بازی‌ها (تسک ۲۳):** سرور نگاشت `socketUsernames` (socketId → username از DB هنگام احراز JWT) + فیلد `names` در `roomUpdate`؛ لیدربورد/تاس/نوار پول وگاس و چت اسم واقعی را نشان می‌دهند؛ «You» برای خود کاربر؛ مهمان بدون لاگین همچنان `Player N`؛ پیام‌های join/leave با اسم واقعی.
- [x] **دکمه Share (تسک ۲۶):** به‌جای کپی کد — `navigator.share` با لینک `origin/game/CODE` + فالبک کپی (فیدبک «Code copied!»). دکمه کپی کوچک هدر حفظ شد.
- [x] **ادیت پروفایل (تسک ۲۴):** `PATCH /auth/me` (JWT) با اعتبارسنجی یوزرنیم لاتین + یکتا؛ `updateUser` در AuthContext؛ فرم MUI در کارت هویت صفحه پروفایل (خطای 409-readable، پیام موفقیت).
- [x] **سیستم Undo (تسک ۲۷):** اسنپ‌شات state قبل از هر `gameAction` (فقط تخته‌نرد/وگاس) در استک per-room (سقف ۵۰)؛ رویداد سوکت `undo` با اعتبارسنجی سمت سرور؛ دکمه Undo در نوار نوبت (فقط نوبت خودت).
  - **تصمیم دامنه (مستند):** undo = «take-back» فقطِ **آخرین اکشن** و فقط توسط همان بازیکن (بالای استک) تا وقتی نفر بعدی اقدامی نکرده — برگرداندن حرکت حریف/اقدام قبلی ممکن نیست. تاس‌ریزی undo نمی‌شود. استک در شروع بازی/rematch/nextRound/پایان بازی پاک می‌شود.

## بچ ۱۳ — سخت‌سازی امنیتی (تکمیل شد — ۱۹ مرداد)

- [x] **multer → 2.2.0 (رفع CVE-2026-3304):** `overrides: { "multer": "^2.1.0" }` در package.json ریشه — DoS با پاکسازی ناقص فایل‌های موقت.
- [x] **اجرای کانتینرها با کاربر غیر root:** در استیج runner هر دو Dockerfile — `chown -R node:node /app` + `USER node` + `ENV HOME=/tmp` (امتیازدهی کمینه؛ اگر اپ هک شود attacker root ندارد).
- [x] **ci.yml:** `persist-credentials: false` روی actions/checkout — credential گیت‌هاب در runner ذخیره نمی‌شود.
- [x] **حذف هش bcrypt هاردکد از history.service.ts:** هش placeholder به‌صورت runtime (`bcrypt.hashSync` در سطح ماژول) محاسبه می‌شود — اسکنر دیگر رشته‌ی شبیه secret نمی‌بیند.
- [x] **ریت‌لیمیت احراز هویت:** `@nestjs/throttler@6.5` — گارد سراسری ۲۰ درخواست/دقیقه + محدودیت سخت‌تر ۵/دقیقه روی `/auth/login` و `/auth/otp/request`. **نکته:** `trust proxy` در main.ts فعال شد (پشت Caddy) تا throttle بر اساس IP واقعی هر کاربر باشد نه IP مشترک پراکسی.

## وضعیت فعلی
پلتفرم BaziGB اکنون دارای ۴ بازی فعال است. زیرساخت موتور بازی (Engine) برای پشتیبانی از نوبت‌های چند مرحله‌ای ارتقا یافته است.
- **Engine Core:** پشتیبانی از سیستم `applyAction` برای حرکت‌های متوالی در یک نوبت (بدون تغییر اجباری نوبت).
- **Vegas Plugin:** پیاده‌سازی کامل بازی استراتژیک وگاس با سیستم تاس‌ریزی دستی، قوانین کامل کارت‌های پول (بر اساس بازی رسمی Vegas + قوانین خانگی پروژه).
- **Server Gateway:** اضافه شدن هندلر `gameAction` + هندلر `nextRound` برای راندهای وگاس.

## کارهای انجام شده
- [x] پیاده‌سازی منطق و رابط کاربری بازی وگاس (Vegas).
- [x] ارتقای سرور برای پشتیبانی از پروتکل `gameAction`.
- [x] رفع مشکل همگام‌سازی تاس در بازی‌های تاس‌محور.
- [x] بهبود سیستم تعاملات تخته‌نرد با استفاده از اکشن‌های جدید.
- [x] **بازنویسی منطق کارت‌های پول وگاس** (بچ ۱۰): ۵۴ کارت (۶ تا از هر ارزش ۱۰هزار تا ۹۰هزار)، هر راند شافل و توزیع رندوم در ۶ استک دوتایی؛ استک‌ها بر اساس مجموع ارزش چیده می‌شوند (گران‌ترین روی خونه ۶).
- [x] **قوانین تسویه هر خونه:** نفر اولِ بی‌رقیب کارت بالاتر را می‌برد و نفر دومِ بی‌رقیب کارت پایین‌تر؛ تساوی در هر رتبه = کارت سوخته (از بازی خارج می‌شود).
- [x] **قانون خانگی ۸ تاس:** کسی که هر ۸ تاسش را روی یک خونه بگذارد، هر دو کارت را می‌برد و نفر دوم هیچی نمی‌گیرد (Sweep).
- [x] **لیدربرد بعد از هر راند** + دکمه «راند بعد» (فقط بازیکنان نشسته) + نوار پول هر بازیکن حین بازی.
- [x] **نمایش تفکیکی تاس هر بازیکن** روی هر خونه (رنگ + تعداد + برچسب P1/P2) — رفع ناسازگاری کلیدهای ایندکس/سوکت‌آیدی.
- [x] **رفع باگ skipVegasNoDicePlayers** (کلیدهای `playerDiceRemaining` ایندکس هستند نه socket id).
- [x] **رفع بلوک سفید موبایل هنگام زوم‌اوت:** برچسب‌های Nav در موبایل مخفی شدند + `overflow-x: clip` در globals.css.
- [x] **حفاظت از صندلی بازیکن قطع‌شده:** تماشاگر دیگر نمی‌تواند صندلی بازیکن قطع‌شده را بگیرد — بازپس‌گیری فقط با همان JWT کاربر (`identityOk`) یا تیکت صندلی (`seatKey` در sessionStorage، TTL ۱۰ دقیقه) ممکن است؛ کاربران قدیمی بدون تیکت رفتار legacy دارند.
- [x] **فیکس CI:** اسکریپت build ریشه از `--workspaces` (موازی) به بیلد ترتیبی بر اساس ترتیب وابستگی (engine → بازی‌ها → server → web) تغییر کرد — بدون آن، server قبل از ساخته‌شدن `dist` پکیج‌ها کامپایل می‌شد و CI با TS2307 قرمز می‌شد.

### قوانین نهایی بازی وگاس (بچ ۱۰)
- **کارت‌های پول:** ۵۴ کارت، ۶ نسخه از هر ارزش ۱۰٬۰۰۰ تا ۹۰٬۰۰۰ دلار. هر راند کل دسته شافل می‌شود و ۶ استک دوتایی (۱۲ کارت) به صورت رندوم بسته می‌شود.
- **چیدمان استک‌ها:** استک‌ها بر اساس مجموع ارزش مرتب می‌شوند — گران‌ترین روی خونه ۶، بعدی روی خونه ۵، … کم‌ارزش‌ترین روی خونه ۱. (مجموع مساوی: کارت بالاتر بالاتر می‌نشیند.)
- **تعداد راندها:** دقیقاً ۴ راند (هر راند ۱۲ کارت = ۴۸ کارت از ۵۴ کارت؛ ۶ کارت آخر بدون استفاده می‌مانند). بعد از راند ۴ برنده مشخص می‌شود و راند ۵ وجود ندارد.
- **جریان بازی:** هر بازیکن در نوبتش تاس‌های باقیمانده‌اش را می‌ریزد، یک عدد را انتخاب و همه تاس‌های همان عدد را روی خونه‌ی همان عدد می‌گذارد؛ بقیه تاس‌ها برای نوبت‌های بعدی می‌مانند. وقتی تاس همه تمام شد راند تسویه می‌شود.
- **تسویه:** برنده بی‌رقیب هر خونه کارت بالاتر، نفر دوم بی‌رقیب کارت پایین‌تر. تساوی در صدر = کل استک می‌سوزد؛ تساوی در رتبه دوم = کارت پایینی می‌سوزد؛ خونه بدون تاس = کل استک می‌سوزد.
- **قانون خانگی ۸ تاس:** ۸ تاس روی یک خونه = Sweep (هر دو کارت + انکار نفر دوم). تساوی دو نفره در ۸ = سوخته.
- **پایان بازی:** بعد از راند ۴ برنده = بیشترین پول؛ تساوی = تعداد کارت‌ها؛ باز هم تساوی = مساوی (draw).
- **سوختن کارت‌ها:** کارت‌های سوخته از جریان بازی خارج می‌شوند (در همان راند حذف می‌شوند و برای راند بعد استفاده نمی‌شوند).

## گام‌های بعدی پیشنهادی
۱. **پنل مدیریتی (Admin Panel):** تعریف نقش ادمین (فیلد role + ادعای JWT + گارد) و ساخت Admin API + صفحه `/admin` — تسک‌ها در سیستم تسک تعریف شده‌اند.
۲. **سیستم Undo/Redo:** اضافه کردن قابلیت بازگشت به عقب در بازی‌های استراتژیک.
۳. **تکمیل تعاملات تخته‌نرد:** نهایی کردن سیستم Drag & Drop و قوانین پیشرفته حرکت.
۴. **نوتفیکیشن‌های پوش:** اطلاع‌رسانی نوبت بازی به کاربران.
۵. **بهبود انیمیشن‌ها:** استفاده از فیزیک برای حرکت طبیعی‌تر تاس‌ها.

## تصمیمات باز (Open Decisions) — فعلاً فقط ثبت شده، نیاز به تصمیم‌گیری بعدی
- **الگوریتم حذف/بستن بردها و رکوردها:** اینکه بردها و رکوردهای بازی چگونه از لیدربورد و تاریخچه حذف شوند (حذف نرم + ممیزی + بازگردانی آمار + پاکسازی دورهای با کران) هنوز تصمیم‌گیری نشده است. **این موضوع فقط در اینجا ثبت شده و تا تصمیم بعدی در مورد راهکار و نحوه اجرا، هیچ‌کاری انجام نمی‌شود.**

## پروتکل عملیاتی BaziGB (مهم برای هوش مصنوعی)

برای حفظ نظم و جلوگیری از شلوغی Context، تمام تسک‌ها باید طبق این پروتکل انجام شوند:

۱. **دسته بندی (Batching):** تسک‌ها بر اساس نقشه راه (Roadmap) به گروه‌های حداکثر ۵تایی تقسیم می‌شوند.
۲. **برون‌سپاری (Delegation):** انجام تسک‌ها به ساب‌ایجنت‌ها (`sessions_spawn`) سپرده می‌شود. هر ساب‌ایجنت مسئول یک خروجی مشخص و مستقل است.
۳. **تأیید و ادغام (Verify & Merge):** پس از اتمام کار ساب‌ایجنت‌ها، ایجنت اصلی نتایج را وریفای کرده و با کد اصلی مرج می‌کند.
۴. **بروزرسانی مستندات:** پس از هر بچ، تمام مستندات فنی و فایل `HANDOFF.md` بر اساس تغییرات جدید آپدیت می‌شوند.
۵. **ثبت نهایی (Push):** پس از تأیید نهایی هر بچ ۵تایی، کدها به مخزن GitHub پوش می‌شوند.

---

## نکات فنی برای توسعه‌دهنده بعدی
- برای اجرای کل پروژه، در ریشه دستور `npm install` را بزنید.
- **بیلد همه ورک‌اسپیس‌ها:** `npm run build` در ریشه به‌صورت ترتیبی (engine → game-* → server → web) اجرا می‌شود؛ از `--workspaces` استفاده نکنید چون موازی است و وابستگی‌ها ساخته‌نشده می‌مانند (خطای TS2307 در server). همین ترتیب در Dockerfile هم رعایت شده.
- منطق بازی‌ها را فقط در `packages/games` تغییر دهید.
- انجین در حال حاضر بسیار ساده است و نیاز به سیستم `Undo` و `Redo` دارد.
