# لیست ایشوها و بدهی‌های فنی (Issues & Technical Debt)

این سند برای ردیابی مواردی است که نیاز به اصلاح یا بهبود دارند اما در اولویت لحظه‌ای نیستند.

## بدهی‌های فنی (Technical Debt)
- [x] **Hardcoded Room:** سیستم اتاق‌های پویا و لابی پیاده‌سازی شد. (DONE)
- [x] **Memory Persistence:** وضعیت بازی‌ها اکنون در PostgreSQL ذخیره می‌شود (بچ ۱۴ — تولید). (DONE)
- [x] **Asset Pipeline:** زیرساخت صوتی پایه اضافه شد. (DONE)
- [x] **Turn Finalization:** پایان نوبت در بازی‌های چند-حرکتی با دکمه «End Turn» مدیریت شد. (DONE)
- [x] **Undo:** سیستم Undo (بازگرداندن آخرین اکشن توسط خود بازیکن) — بچ ۱۲. (DONE)
- [ ] **Socket Identity:** سوکت‌ها هنوز به طور کامل با هویت JWT بازیکنان گره نخورده‌اند (فعلاً از socket.id استفاده می‌شود).
- [ ] **Scaling:** برای مقیاس‌پذیری و توزیع سوکت‌ها بین چندین سرور نیاز به Redis داریم.
- [ ] **Error Handling:** مدیریت خطا در Gateway سرور هنوز ابتدایی است.
- [ ] **تغییر رمز کاربران:** سیستم change-password ساخته نشده (ادمین اصلی رمز موقت `Boxavier@1405!` دارد).
- [ ] **کلید SSH دیپلوی:** سرور پروداکشن به GitHub وصل نیست — دیپلوی‌ها با rsync + بیلد طولانی (~۲۰ دقیقه) انجام می‌شود.
- [ ] **Dev لوکال server:** schema به PostgreSQL تغییر کرده — اجرای لوکال سرور بدون Postgres ممکن نیست (SQLite dev.db منسوخ).
- [ ] **لوگوی رسمی:** داخل لوگو «BaziG3» نوشته شده — برای هماهنگی با برند (BaziGB) نیاز به تصمیم محصول (سفارش لوگوی جدید یا ماندن همین).

## باگ‌های شناخته شده
- [x] مشکل رفرش صفحه وب و قطع اتصال (مرتفع شد).
- [x] عدم دریافت پیامک OTP برای 09120209862 (شارژ کم می‌شد ولی پیامک نمی‌رسید — مشکل سمت اپراتور/فیلترینگ) — **برطرف شد** (تأیید کاربر، ۲۰ مرداد).
- [ ] انجین فعلی حرکت‌های تکراری در یک خانه دوز را در سمت سرور به درستی بلاک نمی‌کند (فقط در کلاینت بلاک می‌شود).
- [ ] ناهماهنگی جزئی در همگام‌سازی صدای تاس در زمان ریختن همزمان توسط دو بازیکن.

## پیشنهادات بهبود
- [ ] پیاده‌سازی سیستم **Timer** برای هر نوبت جهت جلوگیری از معطلی بازیکنان.
- [ ] استفاده از **Zod** برای اعتبارسنجی پیام‌های ورودی به سوکت.
- [ ] تکمیل پنل ادمین (ویرایش کاربران، تنظیمات بیشتر سایت، آمار عمیق‌تر).
