# لیست ایشوها و بدهی‌های فنی (Issues & Technical Debt)

این سند مواردی را ردیابی می‌کند که نیاز به اصلاح دارند اما در اولویت لحظه‌ای نیستند. وضعیت بچ‌ها: [HANDOFF.md](HANDOFF.md).

## بدهی‌های فنی (Technical Debt)

### برطرف‌شده ✅
- [x] Hardcoded Room: سیستم اتاق‌های پویا و لابی. (DONE)
- [x] Memory Persistence: وضعیت بازی‌ها در PostgreSQL ذخیره می‌شود. (DONE)
- [x] Asset Pipeline: زیرساخت صوتی پایه. (DONE)
- [x] Turn Finalization: دکمه «End Turn» برای بازی‌های چندحرکتی. (DONE)
- [x] Undo: بازگرداندن آخرین اکشن توسط همان بازیکن — بچ ۱۲ (تخته‌نرد/وگاس).
- [x] Socket Identity: گره‌خوردن سوکت به هویت JWT + بازپس‌گیری صندلی — بچ ۱۵-ب.
- [x] تغییر رمز کاربران: `PATCH /auth/change-password` — بچ ۱۵-ب.
- [x] تایمر نوبت ۱۲۰ ثانیه + end-turn خودکار — بچ ۱۵-ب.
- [x] مدیریت کامل کاربران ادمین (ریست/غیرفعال/حذف/ویرایش) — بچ ۱۶.
- [x] اعتبارسنجی Zod پیام‌های سوکت — بچ ۱۶.
- [x] فیکس باگ دوز (حرکت تکراری) — بچ ۱۶.
- [x] هوشمندسازی تخته‌نرد (Move Hints + اتوماسیون حرکات اجباری) — بچ ۱۸.
- [x] بازطراحی بصری بورد تخته‌نرد (SVG + بافت + مهره ۳بعدی) — بچ ۱۸–۱۹.

### باز 🔴
- [ ] **کلید SSH دیپلوی:** سرور پروداکشن به GitHub وصل نیست — دیپلوی با rsync + بیلد طولانی (~۱۲–۲۵ دقیقه) انجام می‌شود. راه‌حل: ساخت کلید SSH روی سرور و اضافه‌کردن به GitHub + تنظیم remote.
- [ ] **پایداری فایل `.env` سرور:** فایل `.env` در `/opt/bazigb` چند بار گم شده و دیپلوی با خطای «required variable … missing» شکست خورده (بچ ۱۸/۱۹). راه‌حل: بکاپ از `.env` در مسیر امن سرور (مثلاً `/root/.env.bazigb.bak`) + چک‌لیست قبل از بیلد.
- [ ] **Postgres لوکال:** اجرای لوکال سرور نیازمند Postgres است (SQLite منسوخ). راه‌حل: `docker compose up db` یا داکر لوکال.
- [ ] **لوگوی رسمی:** داخل لوگو «BaziG3» نوشته شده — نیاز به تصمیم محصول (سفارش لوگوی جدید یا ماندن).
- [ ] **Scaling:** برای توزیع سوکت‌ها بین چند سرور نیاز به Redis داریم.
- [ ] **Error Handling:** مدیریت خطا در Gateway سرور هنوز ابتدایی است (بیشتر `client.emit('error')` بدون ساختار یکپارچه).
- [ ] **AI شطرنج و وگاس:** فقط دوز و تخته‌نرد AI کامل دارند؛ شطرنج (موتور) و وگاس (لول‌بندی) در دست توسعه است.
- [ ] **UI بازی با کامپیوتر:** دکمه «Play with AI» در اتاق اضافه شده اما انتخاب سطح دشواری (Easy/Medium/Hard) هنوز در UI نیست (سرور آماده است).

## باگ‌های شناخته شده

### برطرف‌شده ✅
- [x] مشکل رفرش صفحه و قطع اتصال. (DONE)
- [x] عدم دریافت پیامک OTP برای 09120209862 (مشکل سمت اپراتور/فیلترینگ) — تأیید کاربر ۲۰ مرداد.
- [x] انجین حرکت‌های تکراری دوز را بلاک نمی‌کرد — بچ ۱۶.
- [x] عدم نمایش مهره‌ها روی بورد تخته‌نرد (هم‌پوشانی منفی) — بچ ۱۹.

### باز 🔴
- [ ] ناهماهنگی جزئی در همگام‌سازی صدای تاس هنگام ریختن همزمان دو بازیکن.
- [ ] چیدمان فوتر در موبایل (اینماد + لینک‌ها) نیاز به تست مجدد در 360px دارد.

## پیشنهادات بهبود
- [x] تایمر هر نوبت (۱۲۰ ثانیه) — بچ ۱۵-ب. (DONE)
- [x] Zod برای اعتبارسنجی پیام‌های سوکت — بچ ۱۶. (DONE)
- [x] Match Settings: مسابقات Best of 3/5 — بچ ۱۸. (DONE)
- [ ] **راهنمای قوانین بازی‌ها (Help):** هیچ صفحه/دیالوگ راهنمای قوانین داخل اپ نیست (مخصوصاً وگاس و تخته‌نرد) — یافته هیوریستیک ۱۰ نیلسن (بچ ۱۷). + صفحه About/راهنما برای سایت.
- [ ] **یکدست‌سازی زبان لیبل‌ها:** لیبل‌های UI ترکیبی فارسی/انگلیسی‌اند (Leaderboard, Refresh, Join, RATING…) — یافته هیوریستیک ۲ (بچ ۱۷). تصمیم: فارسی‌سازی کامل یا دوزبانه‌ی منظم.
- [ ] مدیریت تورنمنت‌ها از پنل ادمین + تنظیمات بیشتر سایت.
- [ ] انتخاب سطح دشواری AI در UI (سه‌حالته) هنگام ساخت اتاق یا درخواست ربات.
